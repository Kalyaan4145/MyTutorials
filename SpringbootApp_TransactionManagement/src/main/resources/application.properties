spring.application.name=My Studengt Application Testing..

server.port : 8086

spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5

logging.pattern.console=%-5level %logger{36} - %msg%n

# PostgreSQL Configuration
#spring.datasource.url=jdbc:postgresql://192.168.1.4:5432/postgres
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=root
spring.datasource.driver-class-name=org.postgresql.Driver

#drop n create table again, good for testing, comment this in production
#spring.jpa.hibernate.ddl-auto=create

#spring.jpa.show-sql=true

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true


#***************************
#   END POINT URLS
#*************************

#*********************************************
#spring boot+ bean scopes
#******************************************
#Access the endpoint: http://localhost:8080/scope/test.


#******************************************
#springboot + transaction management + maven example + postgres db
#***************************************************
# Postman or cURL to test the /api/accounts/transfer
#URL: http://localhost:8080/api/accounts/transfer
#HTTP Method: POST
#Headers: Content-Type: application/x-www-form-urlencoded
#Body : Choose the x-www-form-urlencoded option in Postman and provide the following key-value pairs:
#Key	Value
#fromAccountId:1
#toAccountId:2
#amount:500

#Postman Body Setup
#Select POST as the HTTP method.
#Enter the URL: http://localhost:8080/api/accounts/transfer.
#Navigate to the Body tab in Postman.
#Select the x-www-form-urlencoded option.
#Add the following key-value pairs:
#fromAccountId: 1
#toAccountId: 2
#amount: 500

#Raw JSON Example (if API supports JSON):
#If the API is configured to accept JSON, you can send a raw JSON body instead:

#json
#Copy code
#{
#    "fromAccountId": 1,
#    "toAccountId": 2,
#    "amount": 500
#}
#Make sure to set the Content-Type header to application/json.

#Testing Steps
#Ensure your PostgreSQL database is up and running.
#Insert test accounts into the account table with sufficient balances.
#Use Postman to perform a money transfer using the provided request format.
#Verify the database to confirm the balances are updated correctly in successful cases.


#****************************************************
#spring boot + transaction management + Propagation + maven + postgres db example
#***************************************************
#Run and Test
#Run the Application: Start your PostgreSQL database and the Spring Boot application.
#Endpoint: Access the endpoint via a GET request:
#http://localhost:8080/api/test. 	



#******************************************
#springboot + transaction management + maven example + postgres db
#***************************************************
#Read Committed: GET http://localhost:8080/api/accounts/read-committed
#Repeatable Read: GET http://localhost:8080/api/accounts/repeatable-read
#Serializable: GET http://localhost:8080/api/accounts/serializable


